# JSON 追加导入修复说明（2026-08-31）

## 本次修改

设置页原来的「导入 JSON」会直接用新文件替换整个客户数组。本版改为：

- 按钮显示为「导入 JSON（新增）」；
- 原有客户列表不会被清空；
- 新客户直接追加；
- 同 ID 的客户自动合并；
- ID 不同但公司名称 / 官网 / 店铺链接一致时，也视为同一客户并智能合并；
- 合并时优先保留 CRM 里已经人工维护的非空字段，新 JSON 只补空缺；
- 联系人、标签、产品匹配、跟进备注采用追加去重；
- 新 JSON 不再覆盖现有 AI 历史和市场分析；
- 重新导入的客户会解除对应删除墓碑 / 回收站 / 待删除队列，继续兼容 Supabase 防“旧客户复活”机制；
- Service Worker 缓存升级到 v15，避免 GitHub Pages 继续读取旧版 web.js。

## 示例

当前 CRM 已有 44 个客户，再导入 20 个客户：

- 其中 17 个为新公司；
- 3 个与现有客户重复；

导入后结果为：

- 当前客户总数 61 个；
- 新增 17 个；
- 3 个重复客户合并到原记录；
- 原有 44 个客户不会消失。

## 部署

覆盖 GitHub Pages 仓库中的文件后，在 GitHub Desktop Commit 并 Push。部署完成后浏览器执行 `Ctrl + Shift + R` 强制刷新。

本次核心变更文件：

- `web.js`
- `sw.js`
