# V6.1.2 拉取固定位置修复

本版修复“设置 > 从云端拉取”仍然跳页：

- `applyCloudSettings()` 不再在设置页整页重绘。
- 手动拉取期间用 `_manualCloudPull` 阻止配置同步回调重绘。
- Supabase / CloudBase 拉取按钮设为 `type=button`，并 `preventDefault + stopPropagation`。
- 拉取前记录主内容区和窗口滚动位置，完成后连续两帧恢复。
- Service Worker 缓存升级到 v21。

覆盖 GitHub Pages 后请 Ctrl+F5 强制刷新一次。
