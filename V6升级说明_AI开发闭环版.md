# 外贸工作台 V6：AI 开发闭环版

## 新增能力

- 营销洞察：汇总客户动作、产品/主题热度和下一步建议。
- 关键动作评分：按照透明规则计算 0–100 分开发优先级；每项分数可展开追溯。
- 开发邮件中心：选择客户、语言、目标、语气与产品/OE切入点，生成或手写待审核草稿。
- 邮件审核流程：待审核 → 已审核 → 标记已发送；标记发送后自动写入统一沟通时间线。
- 高意向识别：客户回复、询价、样品请求、会议、内部转介绍、多次打开等形成明确行为信号。
- ChatGPT/AI 协作导入：支持粘贴标准 JSON，把对话中完成的背调、关键词、建议和邮件草稿送入工作台审核。
- 工作台内 AI：继续复用设置中的 OpenAI 兼容 API；未配置 API 时仍可使用手动草稿和 JSON 协作导入。

## 评分规则

评分只用于安排开发顺序，不作为客户事实或最终商业判断。当前规则：

- 客户回复 +30
- 询价/报价需求 +25
- 样品请求 +25
- 会议/到访 +30
- 内部转介绍 +20
- 同一邮件多次打开 +20
- 普通打开 +8
- 邮件送达 +4
- 客户或关键人动态 +10
- 退信/未送达 −15
- 禁止触达 −100，并归零

## AI 接入边界

- 当前聊天窗口不能直接嵌入静态工作台，也不能把 ChatGPT 登录状态当作 API 凭证。
- 工作台内直接生成需要单独配置 OpenAI API Key；Key保存在现有工作台的本机设置中。
- 无 API 时，可以把 ChatGPT 输出的标准 JSON 粘贴到智能开发页导入。
- 所有 AI 导入内容先进入审核中心；不会自动覆盖客户正式字段，也不会自动发送邮件。
