# V6.1.4 JSON 更新合并修复

本版修复“导入了客户更新 JSON，但列表仍显示从未跟进 / 云端状态没有变化”的问题。

## 根因
1. 旧版 `mergeImportedCustomer()` 默认只填空字段，不能可靠刷新已有客户的业务状态。
2. 更关键的是：合并完成后仍保留旧 `updatedAt`。Supabase 拉取时会把云端旧记录判断为更“新”，导致刚导入的 `lastFollowUp`、邮件状态等又被覆盖。

## 修复
- 同 ID 导入视为“明确更新同一客户”。
- `last_followup_date / last_followup / lastFollowUp` 统一映射到正式字段 `lastFollowUp`。
- `lastFollowUp` 只允许向更晚日期推进，不允许旧文件倒退跟进日期。
- 同 ID 时允许更新：最近跟进、跟进周期、开发钩子、开发策略、下一步、客户评级、邮件追踪等业务字段。
- 联系人按 Email / WhatsApp / Phone / LinkedIn / 姓名识别同一人，同 ID 更新可补全或纠正联系人，不再轻易生成重复联系人。
- tags / productMatches / activeKeywords / productKeywords 继续采用去重追加。
- 每次成功合并强制刷新 `updatedAt` 和 `_ftImportedAt`，确保 Supabase 能识别并同步本次更新。
- 仅通过“同公司名/网址”命中的记录仍采用保守合并，避免误覆盖人工维护数据。
- 导入按钮文案改为“导入 JSON（新增 / 更新）”。

## 结果
今后 ChatGPT 生成的客户状态更新 JSON，只要带现有客户 ID，导入后 `lastFollowUp` 会正确显示，工作台不会继续显示“从未跟进”，并能正确同步到 Supabase。
