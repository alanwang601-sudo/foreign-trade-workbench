# V6.1.3 客户名固定显示修复

修复客户详情弹窗向下滚动后看不到公司名的问题。

- 弹窗固定标题由“客户详情”改为“客户详情 · 公司名”。
- 公司名称兼容 `name` 与 `company_name` 字段。
- 每次打开客户详情时，详情内容自动回到顶部。
- Service Worker 缓存版本升级为 v22，避免继续加载旧版 `web.js`。

本次不修改 CRM 数据结构和 Supabase 数据。
