# V6.1.1 拉取不跳页修复

修复日期：2026-09-19

## 修复内容

- 修复“设置 → 从云端拉取”成功后设置页整页重渲染、滚动条跳回顶部的问题。
- Supabase 拉取完成后只更新连接状态和云端状态灯，不再调用整页 `render()`。
- CloudBase 的同类手动拉取也同步修复。
- Service Worker 缓存版本由 v19 升级到 v20，避免 GitHub Pages 更新后继续使用旧 web.js。

## 数据行为不变

- 拉取仍会正常把云端数据合并到本地 state/localStorage。
- 切换到“客户管理”“日历”等页面时，会自然读取最新数据。
- 不会清空或重新上传 Supabase 数据。

## 部署

将本压缩包文件覆盖原 GitHub Pages 仓库文件即可。首次更新后建议刷新页面一次；新的 Service Worker 会接管 v20 缓存。
