# 外贸工作台 V6.1 · Supabase Free 稳定版接入指南

## 这次为什么会暂停
旧版 Supabase **不会在页面刷新后自动重连**。即使你每天打开工作台，只要没有进入“设置”重新点一次“连接 Supabase”，工作台就可能一直只读写浏览器本地数据，Supabase 看不到数据库活动。Free 项目持续低活跃后会被平台暂停。

V6.1 已改成：只要曾经成功配置过 Supabase，打开工作台会自动重连；云端失败时保留本地数据，并在顶部显示云端状态。

## 新项目恢复步骤
1. 在 Supabase 新建一个 Free 项目。
2. 打开 SQL Editor，执行 `SUPABASE_FREE_V61_初始化.sql`。
3. 在工作台 → 设置，填写新的 Project URL 和 publishable key，点“连接 Supabase”。
4. **先确认顶部显示“云端在线”**，再点“上传到云端”。
5. 手机端填同一 URL + key，连接后会安全合并。

## 免费保活（推荐）
V6.1 包含 `.github/workflows/supabase-keepalive.yml`，每天 2 次只读取一条 `health_check` 记录。

在 GitHub 仓库：Settings → Secrets and variables → Actions → New repository secret，新增：
- `SUPABASE_URL`：例如 `https://xxxx.supabase.co`
- `SUPABASE_PUBLISHABLE_KEY`：项目 publishable key

部署包含 `.github/workflows/` 后，可以在 Actions 页面手动点一次 **Run workflow** 验证；成功后再依赖定时任务。

## V6.1 数据保护
- Supabase 打不开时，不会把“连接失败”当作“云端空库”。
- 拉取前/上传前自动保存一份最近的本地安全快照。
- 设置页可点“恢复安全快照”；恢复只改本机，不自动覆盖云端。
- `aiHistory` / `marketAnalyses` 改为按 ID 合并，不再因云端空表直接清空本地。
- JSON 导入继续采用客户追加/去重，同时会增量合并 `calendar.todos`。
- 旧版约 23 条客户记录出现的 notes 单字符污染，会在 V6.1 首次启动时自动清理。

## 安全提醒
当前为了兼容你现有的纯前端架构，业务表仍允许 anon key 读写。publishable key 不是密码；如果 RLS 允许 anon 访问，拿到 URL + key 的人理论上可以访问业务数据。

这和 Free/Pro 无关。后续建议单独做一个 V6.2：加入 Supabase Auth，只允许你的登录账号访问客户数据。

## 日常判断
顶部：
- `☁️ 云端在线`：正常同步。
- `☁️ 云端异常`：本地仍可继续工作，不要清缓存；到设置页点“健康检查”。
- `☁️ 本地模式`：尚未配置 Supabase。
