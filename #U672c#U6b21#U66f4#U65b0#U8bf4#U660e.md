# 本次更新说明（开发作战信息 · 产品匹配 · 客户评级独立字段）

> 日期：2026-08-30 ｜ 覆盖文件：`web.js`、`styles.css`、`sw.js`
> 缓存版本已升到 `ftw-cache-v11`，**更新后需要按一次 Ctrl/Cmd + Shift + R 强制刷新**（或 F5 两三次），否则浏览器会用旧缓存。

---

## 一、需要覆盖哪些文件

只需要替换这 **3 个文件**，其余文件（index.html / web.html / sb-sync.js / pg-sync.js / cloudbase-patch.js / manifest.json / 图标）都不用动：

| 文件 | 说明 |
|---|---|
| `web.js` | 全部业务逻辑（本次主要改动） |
| `styles.css` | 新增作战卡片、产品匹配表格、评级徽章配色 |
| `sw.js` | 缓存版本号 → `ftw-cache-v11`（不换就看不到新界面） |

操作：把这三个文件拖进 GitHub Desktop 打开的仓库文件夹覆盖 → 左下角写 summary（如「开发作战信息+产品匹配」）→ Commit → Push。约 1-2 分钟后刷新网页生效。

**你的数据不会丢**：所有客户、日历、待办都存在浏览器本地 localStorage 里，换文件不会影响。

---

## 二、本次改了什么

### 1. 「开发作战信息」固定 4 项

以前只有「下一步动作」一个孤零零的框，现在是固定 4 项，打开客户第一眼就能看全：

```
① 开发阶段   —— 下拉选择（12 个阶段）
② 开发钩子   —— 为什么【现在】值得联系他
③ 开发思路   —— 整体打法：先找谁、主打什么、报价策略
④ 下一步动作 —— 下一步具体做什么（蓝色高亮）
```

打开客户详情就能看到，4 项即使没填也显示"未填写"，提醒你补齐，不会再漏。

**阶段下拉做了双份同步**：基本信息区有一个「阶段」，作战卡片里也有一个，改任意一个另一个自动跟着变，不用担心保存后阶段被回退。

### 2. 新增「产品匹配」模块（支持多条）

和联系人一样可以无限加行，每行 5 个字段：

| 字段 | 例子 |
|---|---|
| 客户产品 | Prime Guard PDF76160 |
| 昊威产品 | HWA-068 |
| OE 号 | FD-4615 / BC3Z9N184B |
| 匹配类型 | 直接交叉 / 同型号替代 / 功能替代 / 潜在匹配 / 待确认 |
| 优先级 | A+ / A / B / C |

客户详情页会渲染成一张表格，直接回答"**我们能卖他什么**"。

> 老客户之前填的「核心产品匹配」文本不会丢：详情页会标成「历史核心匹配」继续显示；打开编辑时会自动拆成一条结构化条目，你确认无误保存即可。

### 3. 客户评级升级成独立字段 `customer_grade`

以前 A+ 只是一个标签（tag），现在是一个真正独立的字段，写在客户档案里，专门用来做筛选。

- 列表页每个客户名后面直接显示 ⭐ 评级徽章，颜色分等级（A+ 红橙渐变 / A 橙 / B 青绿 / C 灰）
- 客户详情页顶部和基本信息区都显示评级 + 对应跟进周期
- **标签（tags）照旧保留**，用来放"A+战略客户""展会客户"这类描述性标签，两者各司其职

### 4. 筛选器加了「≥ N 天没跟进」

这是你点名要的场景：**"把所有 A+ 且 7 天内没跟进的客户找出来"**。

现在客户管理页顶部筛选区是：

```
[搜索] [阶段] [国家] [行业] [全部评级] [🔔仅看需跟进] [未跟进：不限 ▾] [重置]
```

- 选「⭐ A+」+「≥ 7 天没跟进」→ 立刻列出所有 A+ 且一周没动的客户
- 「未跟进」可选 3 / 7 / 14 / 30 / 60 / 90 天
- 顶部实时显示"显示 X / Y"以及当前生效的筛选条件组合
- 「重置」一键清空所有筛选

其他联动：
- 列表的"跟进"列现在显示"上次日期 + **N 天没跟**"，冷掉的客户一眼看出来
- 客户名下方直接显示"➡ 下一步动作"，不点开就知道该干嘛
- 配了产品匹配的客户名后有个 🔧 徽章，鼠标悬停能看到配了几条

### 5. AI 背调现在会直接吐出整套餐战信息

深度背调的提示词改了，AI 除了老字段外，还会直接给：

- `customer_grade`：自动评级 A+/A/B/C
- `development_hook` / `development_strategy` / `next_action`：钩子、思路、下一步
- `product_matches[]`：客户产品 ↔ 昊威型号 ↔ OE 对照，最多 5 条

背调完点「存为新客户」，这些全部自动落进客户档案，不用手抄。
点「更新到现有客户」时只会**补齐空缺**，不会覆盖你已经手填的评级和动作。

### 6. AI 一键填充支持完整 JSON

ChatGPT 给你的 JSON 直接粘进去点「解析填充」，现在支持这套完整字段：

```json
{
  "company_name": "Motorparts Distribution LLC",
  "country_region": "United States",
  "industry": "Auto Parts",
  "website": "https://motorparts.com",
  "store_listing_url": "https://drom.ru/motorparts",
  "source": "AI获客",
  "source_channel": "行业市场",
  "customer_grade": "A+",
  "development_stage": "已报价",
  "development_hook": "近期在招采购，且刚上架 Ford 新款滤清器",
  "development_strategy": "先攻 Strategic Sourcing，主打 HWA-068，用 OE 对照表切入",
  "next_action": "发 Ford 滤清器报价单并附 OE 对照表",
  "followup_days": 3,
  "tags": ["A+战略客户"],
  "product_matches": [
    { "customer_product": "Prime Guard PDF76160", "hwa_product": "HWA-068",
      "oe": "FD-4615 / BC3Z9N184B", "match_type": "直接交叉", "priority": "A+" }
  ],
  "contacts": [
    { "name": "Maria Garcia", "title": "Purchasing Manager", "email": "maria@motorparts.com",
      "phone": "+1-555-0100", "role": "Purchasing", "priority": "P1",
      "credibility": "已验证", "linkedin": "in/mariagarcia" }
  ]
}
```

容错说明（写错也不怕）：
- 评级写 `a+`、`A+ 战略客户`、`Grade A` 都能识别成 A+
- 字段写成 `dev_hook` / `development_hook` / `开发钩子` 都认
- 产品匹配写数组、单个对象、甚至 `"PDF76160 ↔ HWA-068 / FD-4615"` 这种一行字符串都能拆开
- 匹配类型写英文 `direct cross` 之类会原样保留（下拉里手动再选一次即可）

---

## 三、测试情况

写完后跑了三组自动化测试，全绿：

| 测试 | 项数 | 结果 |
|---|---|---|
| `test-war-product-match.js`（新字段逻辑） | 54 项 | 全通过 |
| `test-smoke-ui.js`（jsdom 真跑一遍页面） | 36 项 | 全通过 |
| `test-contact-fields.js`（联系人字段回归） | 全部 | 全通过 |

冒烟测试里发现并修掉了 2 个真实 bug：
1. AI 填充时 `development_hook` / `development_strategy` 这两种命名没被识别
2. AI 填充阶段后，作战区的阶段下拉没同步 → 保存会把阶段打回"线索"

---

## 四、下一步建议（你说了算）

1. **批量评级**：200 个存量客户一个个点开填 A+/A/B/C 太累。可以加个批量设置评级的功能，或者让 AI 根据「行业 + 预估价值 + 阶段」自动批量打一遍，你再挑着改。
2. **产品匹配库**：你现在每次都要手打 HWA-068 这种型号。可以建一个「我方产品库」，选型号时下拉选，还能反向查"哪些客户配过 HWA-068"。
3. **导出报价单**：产品匹配表格已经齐了，可以一键导出成 Excel / PDF 的 OE 对照报价单。

---

# 追加修复：删除的客户又复活了（重要）

## 问题根因

你删掉客户后，列表里确实消失了 —— 但 **12 秒后的同步轮询又把它们从云端拉了回来**。

原因有两层：

1. **删除只改了本地，从没通知云端**
   `sb-sync.js` 里其实早就写好了 `deleteRecord()` 函数，但代码里**从来没调用过它**。所以 Supabase 上一直完好保存着你"已删除"的客户。

2. **同步合并是纯并集，没有"删除"这个概念**
   ```javascript
   if (localMap.has(c.id)) { ...取较新的... }
   else localMap.set(c.id, c);   // ← 云端有、本地没有 → 直接加回来 = 复活
   ```
   本地删了 → 云端还在 → 下次拉取走 `else` 分支 → 客户原地复活。

## 修复方案：删除墓碑 + 云端真删

| 机制 | 作用 |
|---|---|
| **通知云端删除** | 删除时立刻调用 `deleteRecord`，从 Supabase 真删这条记录 |
| **本地墓碑** | 记下"这个 id 我删过了"+ 删除时间，写进 localStorage。同步拉数据时，凡是墓碑里的 id 一律不采纳，30 天后自动清理 |
| **离线补推** | 断网时删除会进待删队列，联网后自动补删（轮询时执行） |
| **多设备跟随删除** | 你在公司电脑删了，家里电脑同步时会发现"云端没这条了"，自动跟着删掉并提示你 |

### 多设备场景怎么判断的

推送成功后会给客户打一个 `syncedAt` 标记（"这条已同步过"）。同步时发现 **本地有 `syncedAt`、但云端已经没这条了** → 说明是别的设备删的 → 本机跟着删。

新建还没推送过的客户没有这个标记，不会被误删。

### 两道保险（防止误判导致数据丢失）

- 云端整表为空时**不做**跟随删除（避免云端出问题把本地 200 个客户全删了）
- 单次跟随删除**上限 20 条**，超过就跳过并提示

## 顺带加了回收站

删除的客户会在**回收站**保留 30 天，可一键恢复：

**设置页 → 数据管理 → 🗑 回收站（N）**

- 每条显示客户名、阶段、评级、删除时间、"N 天前删除"
- **↩ 恢复**：完整还原（含联系人、产品匹配、跟进记录），并重新同步到云端
- **彻底删除**：抹掉快照，不可恢复（墓碑仍保留到过期，确保云端不会把旧数据拉回来）
- 回收站最多 50 条，超出丢最旧的

## 本次需要覆盖的文件

比上次多 2 个，一共 **5 个文件**：

| 文件 | 说明 |
|---|---|
| `web.js` | 删除入口、墓碑、回收站 |
| `sb-sync.js` | Supabase 合并逻辑 + 真删 + 补推 |
| `pg-sync.js` | CloudBase 合并逻辑同步修复 |
| `styles.css` | 回收站样式（复用了联系人卡片样式，无新增） |
| `sw.js` | 缓存版本 → `ftw-cache-v12` |

**更新后务必 Ctrl/Cmd + Shift + R 强制刷新。**

## 测试

新增两组测试，全绿：

| 测试 | 项数 | 覆盖 |
|---|---|---|
| `test-delete-tombstone.js` | 40 项 | 墓碑写入/拦截/过期清理、离线补推、回收站恢复、上限裁剪 |
| `test-delete-e2e-sync.js` | 19 项 | 真实加载 web.js + sb-sync.js，用 mock Supabase 服务端跑完整流程：推送→删除→再同步不复活→云端残留旧记录被拦→别处删除本机跟随→云端清空不误删 |

端到端测试里还抓到并修掉一个隐藏 bug：`syncedAt` 只写在本地、没随数据推上云端，导致 pull 之后标记丢失、多设备跟随删除失效。
