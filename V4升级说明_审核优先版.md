# 外贸工作台 V4：审核优先版

## 本版保留

- V3 客户数据、联系人、跟进记录、评级和产品匹配字段。
- JSON 追加导入与智能去重。
- Supabase 删除墓碑、回收站和多设备同步逻辑。
- 智能开发、深度背调、市场分析、日历与原设置页。

## 本版新增

- 新首页：今日/本周/本月成果、每日 To-do、AI 每日工作推荐、工作容量。
- 快速记录：支持 WhatsApp、Gmail、LinkedIn、电话和展会信息先进入审核中心。
- 审核中心：原始证据、AI候选关键词、正式关键词、审核结果和操作日志分离。
- 关键词版本：审核后的完整关键词自动替代旧的有效关键词；历史版本可追溯。
- 客户业务赛道：成品滤清器、模具/塑料件、双业务。
- 禁止触达：与评级独立，避免低优先级客户仍被营销活动选中。
- 失败/暂停原因：保存在客户正式档案。
- 客户关系覆盖：采购、产品/研发、管理层、销售/信息源。
- 产品中心：区分目录产品（来源待确认）、昊威自产、外采、合作工厂、开发中和客户需求缺口。
- 已导入产品索引 1,662 条：其中 114 条燃油滤清器总成记录来自已核验清单，12 条保留为待核实；184 条气门室盖记录由两份一致目录合并；10 条油底壳记录保留原始 OE 且不虚构内部编号；其余综合目录产品只录入可直接追溯的内部代码、类别和PDF页码。
- 市场开发 SOP：预置美国跨境电商、澳洲矿区车队和大型滤清器工厂工作流。
- 客户情报日报：保存公司/关键人公开动态、原文和基础关键词，用户决定是否送审。
- 数据健康检查：提示同名、同域名、缺联系方式和缺下一步，不自动合并。

## 数据保护原则

1. 原始证据不可被AI摘要覆盖。
2. AI提取先进入待审核区。
3. 审核后的版本才进入正式客户画像。
4. 客户合并、删除、禁止触达和外部发送保持人工控制。
5. V4扩展数据同时保存在本机和日历同步载荷中，兼容现有 Supabase 同步。
6. 导出的 V4 备份包含 `v4Workspace`；旧版客户 JSON 仍可继续追加导入。

## 仍需外部授权的功能

- Gmail真实读取、创建草稿、发送和回复监控。
- 第三方邮件追踪插件的打开/点击数据。
- 客户公司主页与关键人社媒动态自动抓取。
- ChatGPT直接写入线上CRM的专用受控接口。

这些功能的页面和数据方向已预留，但实际启用需要相应账号授权或官方接口，不能仅靠GitHub Pages静态文件安全完成。

## 首次使用

1. 覆盖部署全部文件，确保同时上传 `v4-workbench.js` 和 `v4-workbench.css`。
2. 浏览器如果仍显示旧界面，刷新两次或清理该站点旧缓存；Service Worker缓存版本已升级。
3. 打开“客户管理”，为重点客户确认业务赛道、失败原因和禁止触达状态。
4. 将 WhatsApp 关键沟通粘贴到首页“快速记录”，审核后再发布到正式CRM。
5. 产品中心先录入或导入最可靠的公司产品资料，并明确标注自产/外采。

## 本次产品资料处理规则

- `HW Valve Cover Catalog-2026.xlsx` 与 `HW昊威气门室盖产品目录.xlsx` 的单元格数据完全一致，因此合并为一套产品，不重复录入，并保留两个来源文件名。
- `昊威燃油滤清器总成(1).xlsx` 优先采用字段更完整的“最终可用清单 (2)”；“待确认”工作表中的项目保持待核实状态。
- `昊威油底壳 目录清单.xlsx` 未提供昊威内部编号，产品中心只使用原表 OE 检索，不生成虚构编号。
- `2025 昊威 目录 可查找版.pdf` 的多栏版面不适合在无人工核对时强行绑定每款产品的 OE 和车型，因此综合索引只导入可靠的产品代码、目录类别和PDF物理页；专业表格中的精确信息会覆盖对应的普通目录索引。
- 目录出现本身不能证明产品是自产还是外采。本次导入的 1,662 条记录全部先标记为“目录产品（来源待确认）”；只有经人工审核后才改为自产、外采或合作工厂。
