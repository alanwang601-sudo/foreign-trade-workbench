# CRM 清空后 JSON 重新导入 / Supabase 上传消失修复

## 本次定位到的根因
上一版为了防止“旧设备把已清空客户复活”，在 Supabase customers 表保留了 `__ft_customers_cleared__` 全量清空标记。

问题在于：重新导入的 JSON 往往保留旧的 `createdAt / updatedAt`。上一版在拉取时只优先看这些旧业务时间，因此会把“刚重新导入、刚上传”的客户误判成“清空之前的旧客户”，随后写入 tombstone 并从本地列表移除。

## 本版修复
1. 同步判断改为取 `updatedAt / createdAt / syncedAt / _ftImportedAt` 中最新时间，不再让旧业务时间覆盖新的同步时间。
2. JSON 导入时写入 `_ftImportedAt`，明确表示这批数据是本次重新导入的新数据集。
3. JSON 导入会撤销同 ID 的本地旧 tombstone、回收站残留和 pending delete，避免旧删除状态误伤重新导入的数据。
4. 用户主动“上传到云端”成功后，会撤销同 ID 的旧 tombstone / pending delete；显式上传被视为合法的重新发布。
5. 云端存在比墓碑更新的记录时，会自动撤销过期墓碑，允许合法恢复。
6. 上传失败不再误报成功，会明确提示失败客户数量。
7. Service Worker 缓存版本升级为 `ftw-cache-v14-reseed-fix`，避免 GitHub Pages 继续使用上一版 JS 缓存。
8. 修复 pg-sync.js 中一个重复 `const st` 的语法隐患。

## 你现在的 44 个客户如何恢复
部署本版并强制刷新后：

- **先不要再次执行“彻底清空全部”。**
- 如果你之前点“上传到云端”时显示成功，那么这 44 条很可能已经写入 Supabase，只是上一版随后在本地误删。先点 **“从云端拉取”**，本版会识别它们的 `syncedAt` 比清空标记更新，并恢复出来。
- 如果云端拉取仍没有 44 条，再重新导入原 JSON，然后点 **“上传到云端”**。本版会把它们当作清空后的新数据，不会再消失。
